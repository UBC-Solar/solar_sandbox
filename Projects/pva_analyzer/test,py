import serial
import time

ser = serial.Serial('/dev/ttyACM0', 115200)
while True:
    # print(ser.readline()[:-1])
    val = int.from_bytes(ser.readline()[:-1], byteorder="big")
    print(val)
    # print(ser.read(4).hex()[:-2])
    # print(ser.readline().decode('utf-8'))

